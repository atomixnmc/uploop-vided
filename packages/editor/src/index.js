/**
 * @uploop/editor — Standalone Video Editor
 *
 * Full editor application for uploop-vided. Launches via CLI (`vite dev`)
 * to provide a browser-based editor with player, timeline, preview,
 * inspector, and media library panels.
 */

export { createEditor, EditorApp } from './editor.js'
