/**
 * SchemaRegistry — JSON Schema validation for tool parameters
 */

export class SchemaRegistry {
  constructor() { throw new Error('@uploop/toolset: SchemaRegistry not implemented') }
  register(name, schema) { throw new Error('not implemented') }
  validate(name, params) { throw new Error('not implemented') }
}
