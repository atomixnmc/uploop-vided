/**
 * @uploop/project — Project Management
 *
 * Handles project folder structure, configuration (project.json),
 * and asset management for uploop-vided projects.
 */

export { Project, createProject, loadProject } from './project.js'
export { ProjectConfig } from './config.js'
