/**
 * VideoFrame wrapper — timestamped image data
 */

export class VideoFrame {
  /** @param {{ timestamp: number, data: ImageData, duration?: number }} opts */
  constructor(opts) { throw new Error('@uploop/media: VideoFrame not implemented') }
}
